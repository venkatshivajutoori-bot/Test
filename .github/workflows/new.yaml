name: dev
on: workflow_dispatch
jobs:
  test:
    runs-on: windows-latest
    steps:
       - run: echo "happy new
  build:
    runs-on: ubuntu-latest
   # needs: test
    steps:
      - name: new
        run: echo "how ru"
      - name: naya
        run: echo "im finre"
  prod:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: new
        run: echo "how ru"
      - name: naya
        run: echo "im finre"
          
