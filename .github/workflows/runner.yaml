name: runner
on: workflow_dispatch

jobs:
  frstjob:
    runs-on: ubuntu-latest
    steps:
      
      - name: os speccs
        run: cat /etc/os-release
  secodjob:
    runs-on: self-hosted
    steps:
      - name: os specs
        run: cat /etc/os-release
