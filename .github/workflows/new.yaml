name: mine
on: workflow_dispatch
jobs:
  test:
    runs-on: windows-latest
    steps:
       - run: echo "happy new"
  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: new
        run: echo "how ru"
      - name: naya
        run: echo "im finre"
  prod:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: new
        run: echo "how ru"
      - name: naya
        run: echo "im finre"
          
