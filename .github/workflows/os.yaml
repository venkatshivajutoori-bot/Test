name: osinfo
on: workflow_dispatch

jobs:
  frstjob:
    runs-on: windows-latest
    steps:
      - name: greet
        run: echo "welcmome
      - name: os info
        run: cat /etc/os-release
      - name: mulipltcmds
       
        
        
