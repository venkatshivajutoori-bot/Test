name: runner
on: workflow_dispatch

jobs:
  frstjob:
    runs-on: ubuntu-latest
    steps:
      
      - name: greet
        run: echo "welcmome"
  secodjob:
    runs-on: self-hosted
    steps:
      - name: os specs
        run: cat /etc/os-release
